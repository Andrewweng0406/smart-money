# 複製這份檔案為 .env（不要提交 .env 到 git），並填入你自己的值。
# 取得 Bot Token：跟 Telegram 上的 @BotFather 對話，用 /newbot 建立機器人。
TELEGRAM_BOT_TOKEN=你的bot_token
# 取得 Chat ID：跟你的機器人對話後，開啟
# https://api.telegram.org/bot<你的TOKEN>/getUpdates 找 "chat":{"id": ...}
TELEGRAM_CHAT_ID=你的chat_id

# 選填：設定後才會產生「AI 盤後籌碼與新聞綜合評語」。
# 到 https://console.anthropic.com/ 建立金鑰。沒設定就會自動跳過這段評語，
# 不影響報告其餘部分的產出。
ANTHROPIC_API_KEY=你的anthropic_api_key

# 選填：設定後才會啟用「LINE 白話文極端警報」（現貨突破Call/Put Wall、
# Net GEX轉負、散戶死亡Loop才會觸發，比Telegram每日報告稀有很多）。
# 用的是 LINE Messaging API 的「廣播（broadcast）」，會發給這個官方帳號的
# 所有好友——個人使用情境下預期只有你自己是好友，效果等同於「傳給我自己」。
# 取得方式：
#   1. https://developers.line.biz/console/ 建立一個 Messaging API 頻道
#   2. 頻道設定頁「Messaging API」分頁，「Channel access token」按發行
#   3. 用手機掃該頻道的 QR Code，把這個官方帳號加為好友（broadcast只會
#      發給好友，沒加好友收不到訊息）
LINE_CHANNEL_ACCESS_TOKEN=你的line_channel_access_token
